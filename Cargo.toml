[package]
name = "authenticator"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.30.0"
totp-rs = "^5.0"
sqlx = { version = "0.8", features = ["runtime-tokio"] }
tokio = { version = "1.43.0", features = ["full"] }

[profile.dev]
debug = 0
strip = "debuginfo"
