[package]
name = "authenticator"
version = "0.1.0"
edition = "2021"

[dependencies]
eframe = "0.30.0"
totp-rs = "^5.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.dev]
debug = 0
strip = "debuginfo"
